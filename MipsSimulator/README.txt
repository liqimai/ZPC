Mips模拟器简介：
	只可以执行add,sub,lw,sw,lh,sh,beq,j共8种指令。
	打开程序后，会提示打开文件，此时选中要执行的二进制文件，即可执行。
	boot.txt为Mips汇编代码，boot.bit为汇编过的可执行文件。
	可以用boot.bit进行测试。
	程序有简单的文本模式，支持所有浙标码。
	内存0xc00以后是显存，写入显存浙标码，就会在屏幕上显示对应的字符。
	boot.bit这个程序的效果就是显示一个字符串。
	程序无法退出，只能ctrl+c强制结束。